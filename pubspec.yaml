name: relay_me
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0  # Nécessaire pour utiliser Firebase
  cloud_firestore: ^4.9.3  # Pour interagir avec Firestore
  firebase_storage: ^11.2.3  # Pour gérer l'upload et la récupération de fichiers depuis Firebase Storage

  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
